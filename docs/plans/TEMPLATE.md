> 狀態：未開始

# <一句話標題>

## 目標

要達成什麼，一到三句。寫成「使用者/開發者能夠……」而不是「重構 X」。

## 背景

為什麼要做這件事。已知的線索：相關檔案路徑、已確認的事實、之前試過什麼。

寫給一個沒讀過這段對話的人看——AI agent 每次都是全新 context，你沒寫的它不知道。

## 改動範圍（白名單）

預期只動這些檔案：

- `src/routes/xxxRoutes.js`
- `tests/xxx.test.js`

**要動範圍外的檔案 → 停下來，先更新這份計劃並說明原因，不要先斬後奏。**

## 不做的事（Non-goals）

明列這次不碰的東西，避免範圍漂移：

- 不重構未提及的模組
- 不改既有測試的斷言讓自己過
- 不 commit / push（除非本卡明示）
- 例：不處理 XXX，那是另一張卡

## 驗收條件

每條都要能用命令或肉眼逐條檢查，**禁止「做好做完整」這種寫不成檢核表的字眼**。

- [ ] `npm test` 全綠（連跑兩次都綠）
- [ ] `curl -s localhost:3001/api/xxx` 回 200 且格式符合 DEVELOPMENT.md §5
- [ ] 新端點的 `@openapi` 區塊已補上，`npm run openapi` 可正常產出
- [ ] 既有功能抽查未破壞：<具體指定一項>

## 驗證結果

> 開發完成後填。每條驗收條件對應實際跑過的命令與輸出摘要。
> 沒有可貼出的證據 = 未完成，不要寫「應該可以了」。

| 驗收條件 | 結果 | 證據 |
|---|---|---|
| | | |

## 回寫

> 完成後依 [README.md](./README.md) 的對照表勾選，沒有要更新的寫「無」。

- [ ] FEATURES.md
- [ ] ARCHITECTURE.md
- [ ] DEVELOPMENT.md
- [ ] TESTING.md
- [ ] CLAUDE.md

## 未盡事項 / 風險

沒有就寫「無」。
